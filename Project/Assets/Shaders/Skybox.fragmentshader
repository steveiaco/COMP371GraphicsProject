#version 330 core
out vec4 color;

in vec3 TexCoords;

uniform samplerCube skybox;
uniform vec4 dayNightPhase;
uniform float dayNightRatio;

void main()
{
color = mix(texture(skybox, TexCoords), dayNightPhase, dayNightRatio);
}
